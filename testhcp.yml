- name: HCP
  hosts: all
  tasks:
  - name: Create a plan-only run for review (with polling)
    hashicorp.terraform.run:
      organisation: "TechXchangeNL"
      workspace_id: "tf-random-pet"
      run_message: "Review infrastructure changes"
      plan_only: true
      poll: true
      state: "present"
