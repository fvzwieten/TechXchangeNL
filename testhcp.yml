- name: HCP
  hosts: all
  tasks:
  - name: Create a plan-only run for review (with polling)
    hashicorp.terraform.run:
      organization: "TechXchangeNL"
      workspace: "tf-random-pet"
      workspace_id: "ws-2MPL1VUyoTo9DtNr"
      run_message: "Review infrastructure changes"
      plan_only: true
      poll: true
      state: "present"
